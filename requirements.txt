# Yn Security Bot Requirements
# Core Framework (Pyrogram only - consistent usage)
pyrogram>=2.0.106
tgcrypto>=1.2.5

# Event Loop Optimization (Linux/Mac only, Windows will skip)
uvloop>=0.17.0; sys_platform != 'win32'

# Database & Utilities
aiosqlite>=0.19.0
python-dotenv>=1.0.0

# HTTP Client for external APIs (if needed)
httpx>=0.27.0

# Image processing (optional, for captcha/media features)
pillow>=10.0.0

# Type checking and development
typing-extensions>=4.5.0
